# Custom Grid Horizontal Layout Manager for RecyclerView

This project implements a custom `LayoutManager` for `RecyclerView` in Android, allowing you to display items in a horizontal grid layout.

## Features
 Coming soon.